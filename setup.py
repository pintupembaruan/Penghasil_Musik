from setuptools import setup, find_packages

setup(
    name="islamic-music-bot",
    version="1.1.0",
    packages=find_packages(),
    install_requires=[
        # Dependensi utama sudah dihandle di workflow untuk kestabilan
    ],
)
